"use client";

import { AlertCircle, CalendarDays, CheckCircle2, Eye, EyeOff, KeyRound, Loader2, Mail, UserRound, UserPlus } from "lucide-react";
import Link from "next/link";
import { type FormEvent, useMemo, useState } from "react";
import { AuthRequestError, registerAccount } from "@/lib/auth";
import styles from "../../auth.module.css";
import { policyChecks } from "./policyChecks";
import { type SubmitState } from "./SubmitState";


export default function RegisterPage() { const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [dateOfBirth, setDateOfBirth] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [submitState, setSubmitState] = useState<SubmitState>("idle"); const [error, setError] = useState<{ code: string; message: string } | null>(null); const policyResults = useMemo(() => { return policyChecks.map((check) => ({ ...check, met: check.test(password), })); }, [password]); const passwordMeetsPolicy = policyResults.every((check) => check.met); const canSubmit = name.trim().length > 0 && email.trim().length > 0 && password.length > 0 && submitState !== "loading"; async function submitRegistration(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const normalizedName = name.trim(); const normalizedEmail = email.trim(); if (!normalizedName || !normalizedEmail || !password) { setError({ code: "missing_fields", message: "Name, email, and password are required." }); setSubmitState("error"); return; } if (!passwordMeetsPolicy) { setError({ code: "password_policy", message: "Password does not meet the policy." }); setSubmitState("error"); return; } setSubmitState("loading"); setError(null); try { await registerAccount({ dateOfBirth, email: normalizedEmail, name: normalizedName, password, }); setSubmitState("success"); setPassword(""); } catch (nextError) { const requestError = nextError instanceof AuthRequestError ? nextError : new AuthRequestError("Registration failed.", 0, "network_error"); setError({ code: requestError.code, message: requestError.message }); setSubmitState("error"); } } return ( <main className={styles.page}> <header className={styles.topbar}> <Link className={styles.brand} href="/"> <span className={styles.brandMark}>RL</span> <span> <strong>RustLearn</strong> <small>Workspace</small> </span> </Link> <nav className={styles.topActions} aria-label="Registration links"> <Link className={styles.navLink} href="/login"> Login </Link> <Link className={styles.navLink} href="/session"> Session </Link> <Link className={styles.navLink} href="/ops"> Operations </Link> </nav> </header> <section className={styles.layout}> <div className={styles.hero}> <p className={styles.eyebrow}>Register</p> <h1>Create your RustLearn account</h1> <p> Start with learner access, verify your email, then grow into teacher, organization, or platform scopes as permissions are granted. </p> <div className={styles.statusList}> <span> <Mail size={16} aria-hidden /> Verification email required </span> <span> <KeyRound size={16} aria-hidden /> Strong password policy </span> </div> </div> <form className={styles.panel} onSubmit={submitRegistration}> <div className={styles.panelHeader}> <UserPlus size={22} aria-hidden /> <h2>Register</h2> </div> {submitState === "success" ? ( <div className={styles.successBox} role="status"> <strong>Registration successful</strong> <span> Email verification is required before login. In local development, the API prints the mock verification link in its logs. </span> <div className={styles.successActions}> <Link className={styles.secondaryLink} href="/login"> Go to login </Link> </div> </div> ) : ( <> <label className={styles.field}> <span>Name</span> <span className={styles.inputShell}> <UserRound size={18} aria-hidden /> <input autoComplete="name" name="name" type="text" value={name} onChange={(event) => setName(event.target.value)} /> </span> </label> <label className={styles.field}> <span>Email</span> <span className={styles.inputShell}> <Mail size={18} aria-hidden /> <input autoComplete="email" inputMode="email" name="email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} /> </span> </label> <label className={styles.field}> <span>Date of birth</span> <span className={styles.inputShell}> <CalendarDays size={18} aria-hidden /> <input name="dateOfBirth" type="date" value={dateOfBirth} onChange={(event) => setDateOfBirth(event.target.value)} /> </span> </label> <label className={styles.field}> <span>Password</span> <span className={styles.inputShell}> <KeyRound size={18} aria-hidden /> <input autoComplete="new-password" name="password" type={showPassword ? "text" : "password"} value={password} onChange={(event) => setPassword(event.target.value)} /> <button aria-label={showPassword ? "Hide password" : "Show password"} className={styles.iconButton} type="button" onClick={() => setShowPassword((current) => !current)} > {showPassword ? <EyeOff size={18} aria-hidden /> : <Eye size={18} aria-hidden />} </button> </span> </label> <div className={styles.policyList} aria-label="Password policy"> {policyResults.map((check) => ( <span className={check.met ? styles.met : styles.unmet} key={check.key}> <CheckCircle2 size={14} aria-hidden /> {check.label} </span> ))} </div> {error ? ( <div className={styles.errorBox} role="status"> <AlertCircle size={18} aria-hidden /> <span> <strong>{error.code}</strong> {error.message} </span> </div> ) : null} <button className={styles.primaryButton} type="submit" disabled={!canSubmit}> {submitState === "loading" ? ( <Loader2 className={styles.spin} size={18} aria-hidden /> ) : ( <UserPlus size={18} aria-hidden /> )} Create account </button> <p className={styles.inlinePrompt}> Already have an account? <Link href="/login">Sign in</Link> </p> </> )} </form> </section> </main> ); }
