"use client";

import { AlertTriangle, Landmark, Loader2, RefreshCw, Send } from "lucide-react";
import { type FormEvent } from "react";
import { type PlatformRewardCandidateItem, type RewardAuditEvent } from "@/lib/admin";
import styles from "../admin-routes.module.css";
import { ContextRow } from "./ContextRow";
import { EmptyState } from "./EmptyState";
import { StatusPill } from "./StatusPill";
import { candidateStatusTone } from "./candidateStatusTone";
import { formatDate } from "./formatDate";
import { formatUnderscoreLabel } from "./formatUnderscoreLabel";
import { type RouteError } from "./RouteError";
import { type SectionState } from "./SectionState";


export function RewardCandidateDetail({ candidate, auditError, auditEvents, auditState, canApprove, decisionAmount, decisionError, decisionReason, decisionState, decisionStatus, onDecisionAmountChange, onDecisionReasonChange, onDecisionStatusChange, onRefreshAudit, onSubmitDecision, }: { candidate: PlatformRewardCandidateItem | null; auditError: RouteError | null; auditEvents: RewardAuditEvent[]; auditState: SectionState; canApprove: boolean; decisionAmount: string; decisionError: RouteError | null; decisionReason: string; decisionState: "idle" | "submitting" | "success" | "error"; decisionStatus: "approved" | "rejected"; onDecisionAmountChange: (value: string) => void; onDecisionReasonChange: (value: string) => void; onDecisionStatusChange: (value: "approved" | "rejected") => void; onRefreshAudit: () => void; onSubmitDecision: (event: FormEvent<HTMLFormElement>) => void; }) { if (!candidate) { return ( <section className={styles.panel}> <div className={styles.panelHeader}> <Landmark size={20} aria-hidden /> <div> <h2>Candidate detail</h2> <p>Select a candidate from the queue to inspect context and audit history.</p> </div> </div> </section> ); } const isFinal = candidate.status !== "teacher_approved" && candidate.status !== "pending_teacher_approval"; const canSubmitDecision = !isFinal && decisionReason.trim().length > 0 && (decisionStatus === "rejected" || (decisionStatus === "approved" && canApprove && decisionAmount.trim().length > 0 && !Number.isNaN(Number(decisionAmount)) && Number(decisionAmount) >= 0)) && decisionState !== "submitting"; return ( <section className={styles.panel}> <div className={styles.panelHeader}> <Landmark size={20} aria-hidden /> <div> <h2>Candidate {candidate.id}</h2> <p>{candidate.student.name} · {candidate.student.email}</p> </div> <StatusPill label={formatUnderscoreLabel(candidate.status)} tone={candidateStatusTone(candidate.status)} /> </div> <div className={styles.detailGrid}> <ContextRow label="Student" value={candidate.student.name} /> <ContextRow label="Course" value={candidate.course.title} /> <ContextRow label="Event type" value={formatUnderscoreLabel(candidate.event_type)} /> <ContextRow label="Submitter" value={candidate.submitter.name} /> <ContextRow label="Teacher approver" value={candidate.teacher_approver?.name || "No approver yet"} /> <ContextRow label="Teacher decision" value={candidate.teacher_decision_reason || "No reason recorded"} /> <ContextRow label="Approved amount" value={candidate.approved_amount ?? "Not set"} /> <ContextRow label="Created" value={formatDate(candidate.created_at)} /> <ContextRow label="Updated" value={formatDate(candidate.updated_at)} /> </div> <div className={styles.textBlock}> <h3>Audit history</h3> <button className={styles.secondaryButton} disabled={auditState === "loading"} onClick={onRefreshAudit} type="button"> {auditState === "loading" ? <Loader2 className={styles.spin} size={15} aria-hidden /> : <RefreshCw size={15} aria-hidden />} Refresh </button> {auditState === "loading" || auditState === "idle" ? ( <p className={styles.muted}>Loading audit events.</p> ) : null} {auditState === "error" ? ( <div className={styles.inlineError} role="alert"> <AlertTriangle size={16} aria-hidden /> <span>{auditError?.message || "Audit history could not be loaded."}</span> </div> ) : null} {auditState === "success" && auditEvents.length ? ( <ol className={styles.auditList}> {auditEvents.map((event) => ( <li key={event.id}> <strong>{formatUnderscoreLabel(event.event_type)}</strong> <span> {event.from_status ? `${formatUnderscoreLabel(event.from_status)} -> ` : ""} {formatUnderscoreLabel(event.to_status)} · {formatDate(event.created_at)} </span> {event.reason ? <small>{event.reason}</small> : null} </li> ))} </ol> ) : null} {auditState === "success" && !auditEvents.length ? <EmptyState text="No audit events were returned for this candidate." /> : null} </div> <form className={styles.decisionForm} onSubmit={onSubmitDecision}> <div className={styles.subsectionHeader}> <h3>Amount decision</h3> {isFinal ? <StatusPill label="Final state" tone="neutral" /> : null} </div> <label> <span>Decision status</span> <select disabled={isFinal} onChange={(event) => onDecisionStatusChange(event.target.value as "approved" | "rejected")} value={decisionStatus} > <option disabled={!canApprove} value="approved">Approve amount</option> <option value="rejected">Reject</option> </select> </label> {!canApprove ? ( <p className={styles.muted}>Approve amount requires the APPROVE_REWARD_AMOUNT platform permission.</p> ) : null} {decisionStatus === "approved" ? ( <label> <span>Approved amount</span> <input disabled={isFinal} onChange={(event) => onDecisionAmountChange(event.target.value)} placeholder="0.00" type="text" value={decisionAmount} /> </label> ) : null} <label> <span>Decision reason</span> <textarea disabled={isFinal} onChange={(event) => onDecisionReasonChange(event.target.value)} placeholder="Explain the amount decision or rejection reason." rows={4} value={decisionReason} /> </label> {!isFinal && !decisionReason.trim() ? ( <p className={styles.muted}>A decision reason is required before saving.</p> ) : null} {decisionError ? ( <div className={styles.inlineError} role="alert"> <AlertTriangle size={16} aria-hidden /> <span>{decisionError.message}</span> </div> ) : null} <button className={styles.primaryButton} disabled={!canSubmitDecision} type="submit"> {decisionState === "submitting" ? <Loader2 className={styles.spin} size={16} aria-hidden /> : <Send size={16} aria-hidden />} Save decision </button> {isFinal ? <p className={styles.muted}>This candidate is in a final state and cannot be changed from this screen.</p> : null} </form> </section> ); }
