"use client";

import { AlertTriangle, Loader2, RefreshCw, Send, ShieldAlert } from "lucide-react";
import { type FormEvent } from "react";
import { type FraudBlockAuditEvent, type FraudBlockItem } from "@/lib/admin";
import styles from "../admin-routes.module.css";
import { ContextRow } from "./ContextRow";
import { EmptyState } from "./EmptyState";
import { StatusPill } from "./StatusPill";
import { formatDate } from "./formatDate";
import { formatUnderscoreLabel } from "./formatUnderscoreLabel";
import { targetLabel } from "./targetLabel";
import { type RouteError } from "./RouteError";
import { type SectionState } from "./SectionState";


export function FraudBlockDetail({ block, auditError, auditEvents, auditState, canRevoke, createError, createEvidence, createReason, createScopeType, createState, createTargetId, onCreateEvidenceChange, onCreateReasonChange, onCreateScopeTypeChange, onCreateTargetIdChange, onRefreshAudit, onRevoke, onSubmitCreate, }: { block: FraudBlockItem | null; auditError: RouteError | null; auditEvents: FraudBlockAuditEvent[]; auditState: SectionState; canRevoke: boolean; createError: RouteError | null; createEvidence: string; createReason: string; createScopeType: string; createState: "idle" | "submitting" | "success" | "error"; createTargetId: string; onCreateEvidenceChange: (value: string) => void; onCreateReasonChange: (value: string) => void; onCreateScopeTypeChange: (value: string) => void; onCreateTargetIdChange: (value: string) => void; onRefreshAudit: () => void; onRevoke: (blockId: number) => void; onSubmitCreate: (event: FormEvent<HTMLFormElement>) => void; }) { const scopeCreateOptions = [ { label: "Select scope…", value: "" }, { label: "Teacher", value: "teacher" }, { label: "Organization", value: "organization" }, { label: "Course", value: "course" }, { label: "Reward policy", value: "reward_policy" }, ]; return ( <section className={styles.panel}> <div className={styles.panelHeader}> <ShieldAlert size={20} aria-hidden /> <div> <h2>{block ? `Block ${block.id}` : "Create block"}</h2> <p>{block ? "Inspect context and audit history." : "Add a new fraud block."}</p> </div> {block ? <StatusPill label={block.revoked_at ? "Revoked" : block.expires_at && new Date(block.expires_at) <= new Date() ? "Expired" : "Active"} tone={block.revoked_at ? "neutral" : "warn"} /> : null} </div> {!block ? ( <form className={styles.decisionForm} onSubmit={onSubmitCreate}> <div className={styles.subsectionHeader}> <h3>New fraud block</h3> </div> <label> <span>Scope type</span> <select onChange={(event) => onCreateScopeTypeChange(event.target.value)} value={createScopeType}> {scopeCreateOptions.map((option) => ( <option key={option.label} value={option.value}> {option.label} </option> ))} </select> </label> <label> <span>Target ID</span> <input onChange={(event) => onCreateTargetIdChange(event.target.value)} placeholder="Optional numeric ID for the scope target" type="text" value={createTargetId} /> </label> <label> <span>Reason</span> <textarea onChange={(event) => onCreateReasonChange(event.target.value)} placeholder="Explain why this fraud block is being created." rows={4} value={createReason} /> </label> <label> <span>Evidence reference</span> <input onChange={(event) => onCreateEvidenceChange(event.target.value)} placeholder="Optional case or ticket reference" type="text" value={createEvidence} /> </label> {createError ? ( <div className={styles.inlineError} role="alert"> <AlertTriangle size={16} aria-hidden /> <span>{createError.message}</span> </div> ) : null} <button className={styles.primaryButton} disabled={createState === "submitting" || !createScopeType || !createReason.trim()} type="submit"> {createState === "submitting" ? <Loader2 className={styles.spin} size={16} aria-hidden /> : <Send size={16} aria-hidden />} Create block </button> </form> ) : null} {block ? ( <> <div className={styles.detailGrid}> <ContextRow label="Scope type" value={formatUnderscoreLabel(block.scope_type)} /> <ContextRow label="Target" value={targetLabel(block)} /> <ContextRow label="Reason" value={block.reason} /> <ContextRow label="Evidence" value={block.evidence_reference || "None"} /> <ContextRow label="Created" value={formatDate(block.created_at)} /> <ContextRow label="Updated" value={formatDate(block.updated_at)} /> </div> <div className={styles.textBlock}> <h3>Audit history</h3> <button className={styles.secondaryButton} disabled={auditState === "loading"} onClick={onRefreshAudit} type="button"> {auditState === "loading" ? <Loader2 className={styles.spin} size={15} aria-hidden /> : <RefreshCw size={15} aria-hidden />} Refresh </button> {auditState === "loading" || auditState === "idle" ? ( <p className={styles.muted}>Loading audit events.</p> ) : null} {auditState === "error" ? ( <div className={styles.inlineError} role="alert"> <AlertTriangle size={16} aria-hidden /> <span>{auditError?.message || "Audit history could not be loaded."}</span> </div> ) : null} {auditState === "success" && auditEvents.length ? ( <ol className={styles.auditList}> {auditEvents.map((event, index) => ( <li key={`${event.id}-${event.event_type}-${event.created_at}-${index}`}> <strong>{formatUnderscoreLabel(event.event_type)}</strong> <span>{formatDate(event.created_at)}</span> {event.reason ? <small>{event.reason}</small> : null} </li> ))} </ol> ) : null} {auditState === "success" && !auditEvents.length ? <EmptyState text="No audit events were returned for this block." /> : null} </div> {!block.revoked_at && canRevoke ? ( <div className={styles.textBlock}> <h3>Revoke block</h3> <button className={styles.primaryButton} disabled={false} onClick={() => onRevoke(block.id)} type="button"> Revoke </button> </div> ) : null} {block.revoked_at ? ( <p className={styles.muted}>This block was revoked and cannot be changed.</p> ) : null} {!block.revoked_at && !canRevoke ? ( <p className={styles.muted}>Revoke requires the MANAGE_REWARD_FRAUD_BLOCKS permission.</p> ) : null} </> ) : null} </section> ); }
