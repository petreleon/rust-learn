"use client";

import { AlertTriangle, ArrowLeft, Building2, CheckCircle2, Loader2, RefreshCw, Search, ShieldCheck, Users } from "lucide-react";
import Link from "next/link";
import { type OrganizationMemberList, type OrganizationWorkspaceItem } from "@/lib/organization";
import styles from "../organization-routes.module.css";
import { MemberErrorState } from "./MemberErrorState";
import { OrganizationMemberCard } from "./OrganizationMemberCard";
import { StatusPill } from "./StatusPill";
import { SummaryCard } from "./SummaryCard";
import { organizationMemberPermissionOptions } from "./organizationMemberPermissionOptions";
import { organizationMemberRoleOptions } from "./organizationMemberRoleOptions";
import { type MemberLoadState } from "./MemberLoadState";
import { type RouteError } from "./RouteError";
import { type SettingsSaveState } from "./SettingsSaveState";


export function OrganizationMembersContent({ assignRoleMessage, assignRoleState, canAssignRoles, canManageMembers, draftSearch, loadState, members, onApplyFilters, onAssignRole, onDraftSearchChange, onPageChange, onPermissionFilterChange, onRefresh, onRemoveMember, onResetFilters, onRoleFilterChange, organization, page, permissionFilter, removeMemberMessage, roleFilter, routeError, }: { assignRoleMessage: string | null; assignRoleState: SettingsSaveState; canAssignRoles: boolean; canManageMembers: boolean; draftSearch: string; loadState: MemberLoadState; members: OrganizationMemberList | null; onApplyFilters: () => void; onAssignRole: (memberId: number, roleName: string) => void; onDraftSearchChange: (value: string) => void; onPageChange: (page: number) => void; onPermissionFilterChange: (value: string) => void; onRefresh: () => void; onRemoveMember: (memberId: number) => void; onResetFilters: () => void; onRoleFilterChange: (value: string) => void; organization: OrganizationWorkspaceItem; page: number; permissionFilter: string; removeMemberMessage: string | null; roleFilter: string; routeError: RouteError | null; }) { if (loadState === "loading" || loadState === "idle") { return ( <section className={`${styles.panel} ${styles.singlePanel}`} aria-live="polite"> <div className={styles.panelHeader}> <Loader2 className={styles.spin} size={20} aria-hidden /> <h2>Loading organization members</h2> </div> <div className={styles.skeletonGrid} aria-hidden> <div className={styles.skeleton} /> <div className={styles.skeleton} /> <div className={styles.skeleton} /> </div> </section> ); } if (loadState === "error") { return <MemberErrorState error={routeError} onRetry={onRefresh} />; } if (!members) { return null; } const delegatedCount = members.members.filter((member) => member.delegated_permission_count > 0).length; const verifiedEmailCount = members.members.filter((member) => member.email_verified).length; const kycReadyCount = members.members.filter((member) => member.kyc_verified).length; const totalPages = Math.max(1, Math.ceil(members.total / members.limit)); const canGoBack = members.offset > 0; const canGoForward = members.offset + members.limit < members.total; return ( <> <section className={styles.workspaceHero}> <div className={styles.workspaceTitleBlock}> <Link className={styles.backLink} href={`/organizations/${organization.id}`}> <ArrowLeft size={17} aria-hidden /> {organization.name} </Link> <p className={styles.eyebrow}>Organization members</p> <h2>Member directory</h2> <p className={styles.muted}> Members load from the organization-scoped directory contract. Invite, role-change, removal, and audit-history actions remain separate route work. </p> </div> <div className={styles.actionRow}> <button className={styles.secondaryButton} onClick={onRefresh} type="button"> <RefreshCw size={17} aria-hidden /> Refresh </button> </div> <div className={styles.permissionRow}> {members.operator_permissions.can_invite_members ? <span className={styles.permissionChip}>Can invite</span> : null} {members.operator_permissions.can_manage_members ? <span className={styles.permissionChip}>Can manage members</span> : null} {members.operator_permissions.can_assign_roles ? <span className={styles.permissionChip}>Can assign roles</span> : null} {!members.operator_permissions.can_invite_members && !members.operator_permissions.can_manage_members && !members.operator_permissions.can_assign_roles ? ( <span className={styles.permissionChip}>View-only directory</span> ) : null} </div> </section> {assignRoleMessage ? ( <section className={`${styles.warningPanel} ${styles.singlePanel}`} role="status"> <div className={styles.panelHeader}> {assignRoleState === "success" ? <CheckCircle2 size={18} aria-hidden /> : <AlertTriangle size={18} aria-hidden />} <h2>{assignRoleState === "success" ? "Role assigned" : "Role error"}</h2> </div> <p>{assignRoleMessage}</p> </section> ) : null} <section className={styles.summaryGrid}> <SummaryCard icon={<Users size={20} aria-hidden />} label="Matching members" value={members.total} /> <SummaryCard icon={<ShieldCheck size={20} aria-hidden />} label="Delegated access" value={delegatedCount} /> <SummaryCard icon={<CheckCircle2 size={20} aria-hidden />} label="Verified emails" value={verifiedEmailCount} /> <SummaryCard icon={<Building2 size={20} aria-hidden />} label="KYC ready" value={kycReadyCount} /> </section> <section className={styles.memberFilterPanel} aria-label="Organization member filters"> <label> <span>Search members</span> <span className={styles.inputWithIcon}> <Search size={17} aria-hidden /> <input onChange={(event) => onDraftSearchChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") { event.preventDefault(); onApplyFilters(); } }} placeholder="Name, email, role, or permission" type="search" value={draftSearch} /> </span> </label> <label> <span>Role</span> <select aria-label="Organization member role filter" onChange={(event) => onRoleFilterChange(event.target.value)} value={roleFilter} > {organizationMemberRoleOptions.map((option) => ( <option key={option.label} value={option.value}> {option.label} </option> ))} </select> </label> <label> <span>Permission</span> <select aria-label="Organization member permission filter" onChange={(event) => onPermissionFilterChange(event.target.value)} value={permissionFilter} > {organizationMemberPermissionOptions.map((option) => ( <option key={option.label} value={option.value}> {option.label} </option> ))} </select> </label> <div className={styles.filterActions}> <button className={styles.primaryButton} onClick={onApplyFilters} type="button"> <Search size={17} aria-hidden /> Apply </button> <button className={styles.secondaryButton} onClick={onResetFilters} type="button"> <RefreshCw size={17} aria-hidden /> Reset </button> </div> </section> <section className={styles.panel}> <div className={styles.sectionHeader}> <h2>Member list</h2> <StatusPill label={`Page ${page + 1} of ${totalPages}`} tone="neutral" /> </div> {members.members.length ? ( <div className={styles.memberGrid}> {members.members.map((member) => ( <OrganizationMemberCard canAssignRoles={canAssignRoles} canManageMembers={canManageMembers} member={member} key={member.id} onAssignRole={onAssignRole} onRemoveMember={onRemoveMember} /> ))} </div> ) : ( <p className={styles.muted}> No organization members match these filters. Reset filters or check whether the member still has a role in this organization. </p> )} <div className={styles.paginationRow}> <button className={styles.secondaryButton} disabled={!canGoBack} onClick={() => onPageChange(Math.max(0, page - 1))} type="button" > Previous </button> <span> {members.total === 0 ? "0 members" : `${members.offset + 1}-${Math.min(members.offset + members.limit, members.total)} of ${members.total}`} </span> <button className={styles.secondaryButton} disabled={!canGoForward} onClick={() => onPageChange(page + 1)} type="button" > Next </button> </div> </section> </> ); }
